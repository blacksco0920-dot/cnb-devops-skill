# 生产与恢复续接入口真实验证

2026-09-09 在 FinAgent 既有双环境上验证 Skill `3b9f3c9`。活动主机包保持 `0.2.0-preview.1`，未重装、重复初始化或升级；候选为提交 `4b90749adc09afba92b75a2bddce04c13a179f3b`、构建 `cnb-bv8-1k2366eoo`。

测试构建 20 个必需阶段通过并生成 ready 候选；用户确认 main 快进后，生产就绪、独立签名、授权发布和原生批准完成。生产构建 `cnb-j5o-1k23828m2` 的 11 个必需阶段通过，原始生产回执 SHA256 为 `6a7fd4c7616c44c1c58604afbb5b2fdb063003790e26b6dd53f0012a9bbef21e`，三服务沿用测试候选 digest，没有生产重建。固定生产 `prepare`、`candidate`、`sign`、`publish` 四个本机动作累计 4.470 秒；这只衡量入口动作，不代表整轮发布时间。

测试和生产各 18 项业务检查通过，覆盖身份、健康、鉴权、上传、客户增读改、规则匹配、历史和真实 PNG OCR。字段提取仍为 MockExtractor；真实 MiniMax、完整 H5 UI、租户隔离与产品目录持久化未由本轮证明。

新恢复入口在两环境完成实际导出、下载和独立本机恢复，PostgreSQL schema、全表数据、序列及 uploads 对账通过，源服务前后状态一致。测试/生产首次窗口分别为 72.750 / 71.593364 秒，恢复回执 SHA256 分别为 `a572d8d40908147e4549bd35194f282d0cb2f5c068bb79ec1d0ed29174dce56d` / `f86e2fcc939f40fd1df5503aa09c47fbbed7742c544041d03e46e7d50cd23c81`。同命令重复续接未重新导出或恢复，导出包及导出/恢复回执的摘要和 mtime 均未变化。范围不含 Redis、可重建 OCR 模型、整机、运行秘密或生产原地回退。

全轮从 `13:34:16Z` 到生产首次恢复完成 `14:14:25.545512Z`，共 2409.545512 秒（约 40.16 分钟）；上一轮为 2394.868 秒。main 人工等待约 17 分 29 秒且与测试恢复重叠，不能从总时间简单扣除；本轮不宣称整体提速。token 未采集。live 完成后新增的安装交接修复 `2322f04` 仅有本地验证，不属于本轮运行版本或云端证据。

本记录只公开非秘密身份、摘要和范围；原始回执由项目私密状态索引保管。FinAgent 本轮结束，FinAgentCrm 共享主机接入仍是下一阶段进行项。
