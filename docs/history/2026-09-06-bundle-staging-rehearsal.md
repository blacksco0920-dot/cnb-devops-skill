# 可复用执行包：测试发布与自动候选实测

2026-09-06。FinAgent 重置 CNB 接入及测试主机后，使用可复用包完成三服务测试发布。本文只保存本轮固定证据和复盘；当前项目状态仍由业务仓库两份部署/状态文档维护。

## 固定版本与结果

| 项目 | 本轮证据 |
| --- | --- |
| Skill / 包 | `bde8bd35b9cac3dace29f33b716257e22e51167a` / `0.1.0-preview.2` |
| 工件锁 SHA-256 | `9a62de1efe9dc5bc8ea8e5525f0fb224023c35454f31ca03a8996652bc85060d` |
| 应用提交 | `0594267c31561a28070aa1965297875f1803e511` |
| CNB | [第二轮构建](https://cnb.cool/laoma-ai/finagent/-/build/logs/cnb-ejk-1k1rir9iu)，23 个阶段成功 |
| TAT | `inv-f8bfnp07k1`，固定正文、目标、执行结果及发布回执核验通过 |
| 候选 | `finagent-candidate-cnb-ejk-1k1rir9iu` 由流水线自动生成，annotations 最后设 ready 并回读；本机 fetch 后 peeled commit 与上述提交一致 |
| 公网 | 六个可用性/发布身份 URL 返回 200，API、H5、OCR 的发布身份与本次提交、build 一致 |
| 耗时 | CNB 列表 API 总 duration `560188 ms`，约 9 分 20 秒；pipeline 字段 `559136 ms` |

日志中的阶段时间约为 `2026-09-06T14:44:36Z` 至 `14:53:54Z`；整体耗时采用 API 的精确 duration，不用阶段端点替代。构建日志需有该私有仓库权限；原始主机回执、权限及传输证据保存在项目负责人的私密目录，未随 Skill 分发。公开记录不含凭据、主机目标或业务数据，也不能单独证明所有原始执行细节。

本次事实与验证元数据更新没有改变发布核心。验收绑定上表的提交和工件锁；后续文档或元数据生成的新锁不会自动继承此次验收，也不要求重新下发本轮已验证的应用。

## 实际执行范围

- 一台重置 Ubuntu 24.04 主机保留预装服务和原生 Caddy，实际完成 Docker/Compose、独立 PostgreSQL/Redis、空基线项目安装及域名接入；不是无预装服务的纯净镜像测试。
- CNB 构建并推送 API、H5、OCR 的完整 digest 映射，固定 TAT 执行迁移前备份、发布和运行/公网核验，再由现有阶段自动产生候选；没有手工补候选。
- 第一轮在迁移前备份失败：846 字节的合法 PG16 空库归档被旧代码的 1 KiB 下限误拒。实际归档校验与真实 PG16 回归确认后，修复为非空且合法的 archive 检查。
- 管理员按审阅摘要更新主机控制程序和同 ID TAT 正文，SDK 严格回读后更新绑定。原控制文件四件已备份，旧不完整快照保留；第二轮通过。此单次操作不构成通用升级能力。

## 四项主要问题及最小修复

这四项是实际运行中定位的问题，不是全部接入改动。更早还补了 first-host bootstrap、项目持久目录配置、独立 Redis、OCR 启动等待，以及原生 Caddy 的回环端口配置。本次仍在接入途中完善包能力，不能称为纯参数复用。

| 实际问题 | 修复与固定来源 |
| --- | --- |
| TCR 个人版资源授权方式与预期不同 | 依据实际仓库和权限粒度校正接入交接；`f217832`，见[接入步骤](../../references/bootstrap.md) |
| TAT 轮询使用的 API 超出已有最小权限 | 改用已授权的任务查询，保留实例/命令/调用身份、实际正文及回执校验；`08efcc58a37ef111186095f29605a6e7ce95596b`，见[调用端](../../assets/cnb-tcr-tat/ci/run-tat-release.mjs) |
| Caddy 从 stdin adapt 的自动隐藏文件名不同 | 仅精确校正该默认项，继续拒绝其他运行配置漂移；`51fad6ac5c250346ecb658ee075b8a1514563f02`，见[Caddy 入口](../../assets/cnb-tcr-tat/host/configure-native-caddy.py) |
| 合法空库 dump 小于 1 KiB | 保留大小上限和归档有效性校验，取消错误的最小容量推断；`bde8bd35b9cac3dace29f33b716257e22e51167a`，见[主机发布器](../../assets/cnb-tcr-tat/host/tat-deploy-test.py) |

## 效率与未验证范围

成功流水线约 9 分 20 秒；第一轮失败耗时 `563505 ms`，约 9 分 24 秒。成功轮的主要阶段为：项目检查 `103177 ms`、API 构建 `119343 ms`、H5 构建 `52232 ms`、OCR 构建 `171945 ms`、TAT 部署验证 `76348 ms`；其余阶段及平台时间另计。

首次工具时钟观测为 `2026-09-06T10:57:53Z`，已晚于最初发现；到约 `14:53:56Z` 成功，仅此观察窗口就约 3 小时 56 分钟，仍未达到本次极速接入目标。它不是完整任务起点，9 分 20 秒也不是首次接入总耗时。

账号、权限、主机准备、人工控制台和文件上传、代码修复都有额外成本，未按阶段完整采集。人工动作台账尚不完整，空数组不代表零次介入；主动工作时间和 token 成本未采集，不估算次数、提速或降本。

本轮仍修改了公共核心以修复真实兼容问题，未证明新项目只改配置即可接通；后续可复用这些修复，但需第二个真实项目验证。测试 push 到候选已经自动完成；首次接入使用了浏览器与管理员操作，未完成仅终端/API的新会话复现。

本轮随后完成了限定合成业务检查；该结果不覆盖完整 H5 UI、真实外部模型调用（Mock/MiniMax）、上传/下载或恢复。具体操作、数据和限制由 FinAgent 项目文档保留，不把局部通过扩大为完整业务验收。

生产执行和独立恢复执行器仍未实现，生成的生产事件继续阻断。首发前空库快照和 `pg_restore --list` 不等于业务数据恢复；完整业务流程、真实外部集成、上传文件等持久数据恢复、第二项目云端隔离均未验收。此前项目专用路径的生产/恢复记录不能替代本包结果。下一步范围见[发布与恢复要求](../../references/release-safety.md#backup-and-restore-acceptance)及[未完成计划](../superpowers/plans/2026-09-06-reusable-deployment-workflow.md)。
